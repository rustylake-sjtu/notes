# A Robust Graph-Based Bathymetric Simultaneous Localization and Mapping Approach for AUVs
## Chapter2
BLSAM：bathymetric SLAM（测深SLAM）

MBES：multibeam echosounders（多波束声呐）

当与基于LIDAR或视觉SLAM系统相比时，测深SLAM具有若干缺点。

1.离散时刻采集的MBES ping信号相互独立，相邻时刻之间无法实现数据关联。

2.MBES测深数据具有很大的不确定性，从条带的中心到边缘部分增加。

3.海底地形的三维空间变化不像陆地或室内环境那样明显。

这些缺点导致了几个严重的问题.第一，由于水下导航系统的位置漂移问题，位置估计通常包含很大的误差.这使得搜索相关的子地图对变得困难，并可能导致错误的环路闭合.第二，在BSLAM中可用的环路闭合通常是稀疏的，因为它们只能在AUV路径段彼此非常接近或相交时产生。第三，由于地形的不确定性和匹配适应性差，在许多地区，测深点云配准容易陷入局部最优。

MBES BSLAM有以下几个严峻的问题需要解决：

1.测深数据由于较低的内点匹配率导致的鲁棒性差

内点匹配率反映了匹配上的点云对占总点云对数的比例。当使用ICP算法在两个点云之间执行数据关联时，初始位置对配准结果有很大影响，尤其是当内点率较低时，由于水下组合导航系统的累积误差，水深点的位置存在很大的不确定性，传统的ICP方法不能很好地解决这一问题处理具有低内点比和高位置不确定性的测深点云之间的配准问题。

2.低匹配适用性测深数据降低数据关联性能

海底可划分为匹配适用性不同的子区域，水深点云配准的性能主要由匹配适用性高的部分决定，匹配适宜性低的部分会降低配准效率和精度。

3.对于无效闭合环路的判断

由于水下导航系统的位置漂移问题，需要在位置不确定域中进行水深数据关联，以搜索真正的环路闭合。任何两个误差椭圆重叠的子图之间都需要进行数据关联，因此，需要在前端给出一个诊断机制，以识别和消除错误的环路闭包。在前端完全排除错误的循环闭包是具有挑战性的。SLAM结果将被图中残留的错误因子严重污染，这要求后端优化器对因子图中的离群值足够健壮。

## Chapter3 MBES子图构建的过程
由于子图主要用于前端的环闭合检测，因此需要考虑以下几个因素：

1.子图应该具有适当的长度，以确保在路径交汇区域有较高的内点匹配率和配准率。

2.为了获得高精度的测深配准，构造的子图应具有较高的匹配适应度。

而为了解决这些问题，我们将从以下几方面展开讨论：

### A.基于MBES数据网格化的地形特征计算

步骤如下：

1.对每个网格内的测深点，计算其三维协方差矩阵 C （通过所有点的坐标与均值坐标之差的内积）

2.PCA分解，最小特征值对应的特征向量被选为网格的法向量。

3.坡度计算，坡度通过法向量的垂直分量与水平分量的比值计算，并归一化到 [0,1]，其对应的角度为0-90°

4.坡向计算，坡向通过法向量的水平分量方向计算，归一化到 [0,1]，其对应的角度为0-360°

### B.子图构建方法

1.初始子图分割

子图划分：将MBES条带分割为连续的子条带，每个子条带包含固定数量的Ping。

网格化处理：对每个子条带进一步划分为网格，计算每个网格的坡度和坡向。

2.匹配适应性评测

特征向量生成：每个子条带生成两种向量：坡度向量和坡向向量。

计算方差：计算坡度向量的方差和坡向向量的方差。

物理意义：高方差表示地形变化显著，适合用于数据关联；低方差（如平坦区域）匹配性能差。

3.动态子图合并

1.高适应性条带合并

若当前子条带的坡度向量计算出来的方差大于阈值或是坡向向量计算出来的方差大于阈值，则将其加入当前子图，并将当前计数n加一。当n大于所设定的阈值时，且子图长度大于最小子图长度阈值时，生成一个子图并将其加入集合。若当前子图长度大于最大子图长度阈值时，强制生成子图，避免因为过长而导致的重叠率下降。

其中子图长度的阈值设定如下:

MBES条带宽度设为d，子图最小长度阈值设置为0.5d，最大长度阈值为d，采取自适应方法可以降低匹配误差。

## Chapter4 基于网格特征向量的粗配准、

步骤如下：

1.提取子图的网格特征向量（坡度+坡向），并根据其方差进行初步筛选，仅保留方差大于阈值的网格块。

2.使用KNN搜索相似网格块，生成候选平移向量。分为两步，第一步为特征距离计算，对通过上一步筛选的网格块，计算其与其他所有网格块的欧氏距离。第二步为KNN筛选，保留距离最小的前 K 个匹配对，存入候选集。

3.通过投票机制选择最优初始位姿。首先会计算每个候选匹配对的平移向量，之后统计所有候选平移向量的密集区域，计算其邻域内其他平移向量的数量，选择得分最高的平移向量作为粗匹配的最终结果。

粗匹配结果作为精匹配（点对面ICP）的初始值，显著降低ICP陷入局部最优的概率。

## Chapter5 通过闭环闭合判断实现高效精细配准

核心目标为：1，精确位姿估计：优化旋转矩阵和平移向量。2，误匹配剔除：过滤低一致性的回环闭合，避免污染SLAM后端优化。

该过程的输入为：粗匹配后的源子图和目标子图，以及粗匹配得到的平移向量；输出为：精细变换（包括旋转矩阵R和平移向量t）以及通过一致性检验的有效回环闭合。

步骤：

1.高适应性区域提取。在这一步中将会剔除低匹配适应性点云，提高配准精度。具体通过领域特征差异计算和DBSCAN聚类完成。

2.点对面ICP精配准。使用高斯-牛顿法或Levenberg-Marquardt迭代求解。

3.误匹配检验机制：

双重一致性检验：

1.地形一致性检验：

首先，计算重叠区域点对的深度差和法向量夹角。随后统计一致性指数，一致性指数为深度差大于阈值或是夹角大于阈值的点对数占全部点对数的比例。当一致性指数大于等于0.6即为通过检验。

2.回环闭合一致性：检查相邻子图匹配结果的几何冲突，若源子图Ms同时匹配到两个相邻目标子图MT1和MT2，则会计算中心偏移量。分别计算源子图在两次匹配中的中心偏移量和两个目标子图的中心偏移量，若二者之差的绝对值大于100m，则判断两者冲突，舍去一致性指数较小的一次匹配，保留另外一次匹配。

可以确保匹配结果与AUV运动轨迹逻辑一致（如避免“跳跃式”错误关联）。

## Chapter6 因子图构建和鲁棒性优化

因子类型：

1.航位推算因子：描述AUV相邻节点间的相对运动约束，由惯导、多普勒测速计和压力传感计联合计算得出。可以描述相邻节点间位姿变化。其协方差矩阵反应航位推算的累计误差。

2.回环闭合因子：描述非连续位姿节点间（回环节点间对应的）空间约束，由精匹配计算得出。其协方差矩阵取决于匹配精度。

因子图模型：

因子图最后的表达式为

![因子图表达式](..\\factor_graph.png "因子图计算式")

其中e<sub>k</sub>为因子误差函数，不同因子计算式不同。而由于因子图本质是稀疏的，故而此处采用了QR分解或Cholesky分解加速求解。

3.鲁棒后端优化

（1）误匹配检测：

由于前端可能会遗漏误匹配（如地形一致性较低但是几何巧合），故而需要进行误匹配的检测。

首先会进行路径形态一致性检验，对每个回环闭合关联的位姿节点提取其前后各n个节点（共 2n+1 个节点），形成局部路径段。然后对于航位推算的该路径段和优化后的该路径段，计算两者之间的Frechet距离（用以度量路径形态差异），若大于阈值，则标记为误匹配。之后对于误匹配集合，逐一剔除并重新优化，统计剩余冲突数量，并选择使剩余冲突最少的剔除方案。